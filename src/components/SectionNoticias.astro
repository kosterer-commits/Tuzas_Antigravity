---
const noticias = [
    {
        id: 1,
        categoria: "Investigación",
        titulo: "TecNM Pachuca Gana Premio Nacional de Innovación Tecnológica",
        extracto:
            "Estudiantes de Ingeniería en Sistemas desarrollaron un sistema de monitoreo ambiental inteligente que fue reconocido a nivel nacional.",
        fecha: "8 Feb 2026",
        imagen: "https://images.unsplash.com/photo-1451187580459-43490279c0fa?q=80&w=2072&auto=format&fit=crop",
        destacado: true,
    },
    {
        id: 2,
        categoria: "Eventos",
        titulo: "Feria de Ciencia y Tecnología 2026",
        extracto:
            "Más de 100 proyectos estudiantiles serán presentados en la feria anual que se realizará del 15 al 17 de febrero.",
        fecha: "7 Feb 2026",
        imagen: "https://images.unsplash.com/photo-1540575467063-178a50c2df87?q=80&w=2070&auto=format&fit=crop",
        destacado: false,
    },
    {
        id: 3,
        categoria: "Académico",
        titulo: "Nuevas Certificaciones Internacionales Disponibles",
        extracto:
            "El TecNM Pachuca amplía su oferta con certificaciones en Cloud Computing, Ciberseguridad y Data Science.",
        fecha: "5 Feb 2026",
        imagen: "https://images.unsplash.com/photo-1522202176988-66273c2fd55f?q=80&w=2071&auto=format&fit=crop",
        destacado: false,
    },
    {
        id: 4,
        categoria: "Deportes",
        titulo: "Equipo Tuza Campeón Regional de Robótica",
        extracto:
            "El equipo representativo logró el primer lugar en la competencia regional de robótica educativa.",
        fecha: "3 Feb 2026",
        imagen: "https://images.unsplash.com/photo-1485827404703-89b55fcc595e?q=80&w=2070&auto=format&fit=crop",
        destacado: false,
    },
    {
        id: 5,
        categoria: "Vinculación",
        titulo: "Convenio con Empresas Líderes en Tecnología",
        extracto:
            "Se firmaron acuerdos con empresas internacionales para prácticas profesionales y bolsa de trabajo.",
        fecha: "1 Feb 2026",
        imagen: "https://images.unsplash.com/photo-1553877522-43269d4ea984?q=80&w=2070&auto=format&fit=crop",
        destacado: false,
    },
];

const categoriaColors: { [key: string]: string } = {
    Investigación: "bg-purple-600",
    Eventos: "bg-blue-600",
    Académico: "bg-green-600",
    Deportes: "bg-orange-600",
    Vinculación: "bg-cyan-600",
};
---

<section class="py-32 bg-gradient-to-b from-white/60 to-gray-50/60">
    <div class="max-w-7xl mx-auto px-6">
        <!-- Header -->
        <div class="text-center mb-16 reveal-on-scroll">
            <h2
                class="text-xs font-bold tracking-[0.2em] uppercase text-gray-500 mb-4"
            >
                Boletines
            </h2>
            <h3 class="text-4xl md:text-5xl font-bold text-gray-900 mb-6">
                Últimas Noticias
            </h3>
            <p class="text-lg text-gray-600 max-w-2xl mx-auto">
                Mantente informado sobre los acontecimientos más relevantes de
                nuestra comunidad tecnológica.
            </p>
        </div>

        <!-- News Grid -->
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
            {
                noticias.map((noticia, index) => (
                    <article
                        class={`group relative overflow-hidden rounded-2xl bg-white shadow-lg hover:shadow-2xl transition-all duration-500 reveal-on-scroll reveal-delay-${index * 100} ${
                            noticia.destacado
                                ? "lg:row-span-2 lg:col-span-2"
                                : ""
                        }`}
                    >
                        {/* Image Container */}
                        <div
                            class={`relative overflow-hidden ${
                                noticia.destacado
                                    ? "aspect-[21/9] lg:aspect-[16/7]"
                                    : "aspect-[16/9]"
                            }`}
                        >
                            <img
                                src={noticia.imagen}
                                alt={noticia.titulo}
                                class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-110"
                            />
                            {/* Overlay Gradient */}
                            <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-transparent opacity-60 group-hover:opacity-80 transition-opacity duration-300" />

                            {/* Category Badge */}
                            <div class="absolute top-6 left-6">
                                <span
                                    class={`inline-block px-4 py-2 rounded-full text-xs font-bold text-white ${categoriaColors[noticia.categoria]} backdrop-blur-sm`}
                                >
                                    {noticia.categoria}
                                </span>
                            </div>

                            {/* Date */}
                            <div class="absolute top-6 right-6">
                                <div class="bg-white/95 backdrop-blur-sm px-4 py-2 rounded-full">
                                    <span class="text-xs font-bold text-gray-900">
                                        📅 {noticia.fecha}
                                    </span>
                                </div>
                            </div>
                        </div>

                        {/* Content */}
                        <div
                            class={`p-6 ${noticia.destacado ? "lg:p-10" : ""}`}
                        >
                            <h4
                                class={`font-bold text-gray-900 mb-3 group-hover:text-blue-600 transition-colors duration-300 ${
                                    noticia.destacado
                                        ? "text-2xl lg:text-3xl"
                                        : "text-xl"
                                }`}
                            >
                                {noticia.titulo}
                            </h4>
                            <p
                                class={`text-gray-600 leading-relaxed mb-4 ${
                                    noticia.destacado ? "text-base" : "text-sm"
                                }`}
                            >
                                {noticia.extracto}
                            </p>

                            {/* Read More Link */}
                            <a
                                href={`${import.meta.env.BASE_URL}noticias/${noticia.id}`}
                                class="inline-flex items-center gap-2 text-sm font-bold text-blue-600 hover:text-blue-700 transition-colors group/link"
                            >
                                Leer más
                                <svg
                                    class="w-4 h-4 transform group-hover/link:translate-x-1 transition-transform"
                                    fill="none"
                                    stroke="currentColor"
                                    viewBox="0 0 24 24"
                                >
                                    <path
                                        stroke-linecap="round"
                                        stroke-linejoin="round"
                                        stroke-width="2"
                                        d="M9 5l7 7-7 7"
                                    />
                                </svg>
                            </a>
                        </div>

                        {/* Hover Effect Border */}
                        <div class="absolute inset-0 border-2 border-transparent group-hover:border-blue-600 rounded-2xl transition-all duration-300 pointer-events-none" />
                    </article>
                ))
            }
        </div>

        <!-- View All Button -->
        <div class="mt-16 text-center reveal-on-scroll reveal-delay-500">
            <a
                href=`${import.meta.env.BASE_URL}noticias`
                class="inline-flex items-center gap-3 px-8 py-4 bg-black text-white rounded-full font-bold hover:bg-gray-800 hover:scale-105 transition-all duration-300 shadow-lg hover:shadow-2xl"
            >
                Ver Todos los Boletines
                <svg
                    class="w-5 h-5"
                    fill="none"
                    stroke="currentColor"
                    viewBox="0 0 24 24"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
                </svg>
            </a>
        </div>
    </div>
</section>

<style>
    /* Additional hover effects for news cards */
    article {
        transform: translateY(0);
    }

    article:hover {
        transform: translateY(-8px);
    }
</style>
